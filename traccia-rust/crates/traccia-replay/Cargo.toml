[package]
name = "traccia-replay"
version = "0.1.0"
edition = "2021"

[dependencies]
traccia-core = { path = "../traccia-core" }
traccia-event = { path = "../traccia-event" }
traccia-evidence = { path = "../traccia-evidence" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
